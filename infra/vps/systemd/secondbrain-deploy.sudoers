# Install with visudo -cf, owner root:root, mode 0440.
# The root-owned wrapper validates its exact subcommand and SHA before acting.
brain-deploy ALL=(root) NOPASSWD: /usr/local/sbin/secondbrain-operator *
