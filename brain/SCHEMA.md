# SCHEMA

Мозг = файлы. Задачи только на стол (`C:\Cursor\Tasks` и VPS desk). Сырьё не переписывать.

## Дерево

```
brain/
  SCHEMA.md          # это
  wiki/              # gitignore, правда
    INDEX.md         # старт: ты, бюро, клиенты, фокус
    people/
    clients/
    decisions/
  raw/               # gitignore, оригинал
    _inbox/          # ещё не разобрано
    meetings/        # созвоны и живые
    tg/              # с бота
```

Правило: **не разобрал = не было**. Inbox пустой только после wiki + задач.

## Куда сгружать

| Откуда | Как | Куда |
|---|---|---|
| Голос в @BuBuBuLu_Bot | чекай (VPS inbox + CUDA whisper) | `raw/tg/` потом wiki |
| Zoom / Meet / Teams / trueconf | файл в бота или `raw/_inbox/` | `raw/meetings/YYYY-MM-DD-slug.*` |
| Телефон / живая встреча | диктофон -> бот или `_inbox/` | `raw/meetings/` |
| Текст в бота / чат Cursor | чекай или сразу wiki | wiki + стол |
| Конфа / эфир | скачанный файл -> бот | `raw/meetings/` |

Имена файлов встреч: `YYYY-MM-DD-кто-о-чём.md` + рядом исходник `.ogg/.m4a/.mp4` если был.

## Встречи: whisper сейчас, Plaud потом

Сейчас: локальный CUDA whisper (`C:\Cursor\skills\transcribe-audio-local`). Транскрипт класть в тот же `raw/meetings/`, не размазывать.

Plaud не покупаем. Если появится - **тот же** `raw/meetings/`, тот же ingest. Не отдельный контур.

## Ingest (агент)

1. Сырьё в `raw/` (не править текст записи).
2. Саммари: о чём, договорённости, открытые вопросы.
3. Wiki: люди / клиенты / решения. Имена свернуть с `wiki/INDEX.md`. Не выдумывать клиентов.
4. Задачи на стол, не в wiki-чеклисты.
5. Убрать из `_inbox/`.

Чекай бота = шаг 0 (`scripts/brain_ingest_tg.py` кладёт сырьё в `raw/tg/`). Потом wiki, если там встреча/решение. В бота квитанция: `сохранил → wiki/…, стол id`, не «ок, понял».

Скрипты: `brain_lint.py` (сироты/битые имена), `brain_digest.py` (утро/вечер + зависшие), `brain_receipt.py`. Отказы: `wiki/declined.md`.

## Утро / вечер

Команды **утро** и **вечер**: читать `wiki/INDEX.md` + стол + календарь. Не выдумывать план из воздуха.

- Утро: фокус дня, что горит, что в календаре. Дайджест в бота.
- Вечер: что было / что открыто / что в wiki дописать. Дайджест в бота.

## Стек (не трогать)

- Один TG-бот: `@BuBuBuLu_Bot`. Чекай = VPS, не getUpdates.
- Стол-мастер: VPS. Сайт `buro1.tech` на REG.RU стола не несёт.
- Whisper: домашняя CUDA. VPS только очередь.
- Календарь: Яндекс, MCP `user-yandex-calendar`.
- `@katervl_bot` - отдельный, только бронь катеров, не мозг.
