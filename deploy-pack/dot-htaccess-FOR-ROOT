RewriteEngine On

# API Якоря - не отдавать в SPA
RewriteRule ^api/ - [L]

# Статика как есть
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback
RewriteRule ^ index.html [L]
